/* ==============================
   VARIABLES CSS - IDENTIDAD
   ============================== */
   :root {
    --color-primary: #6d28d9;      /* Morado oscuro */
    --color-primary-light: #8b5cf6;
    --color-dark: #0b0b0f;         /* Negro tecnológico */
    --color-dark-soft: #141420;
    --color-light: #f1f1f5;
    --color-gray: #9ca3af;
  
    --font-main: 'Roboto', sans-serif;
  }
  
  /* ==============================
     RESET & BASE
     ============================== */
  *,
  *::before,
  *::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }
  
  html {
    font-size: 16px;
  }
  
  body {
    font-family: var(--font-main);
    background-color: var(--color-dark);
    color: var(--color-light);
    line-height: 1.6;
  }
  
  /* ==============================
     HEADER & NAV
     ============================== */
  header {
    background-color: var(--color-dark-soft);
    padding: 1.5rem 3rem;
  }
  
  nav {
    display: flex;
    justify-content: center;
  }
  
  nav ul {
    list-style: none;
    display: flex;
    gap: 2rem;
  }
  
  nav a {
    text-decoration: none;
    color: var(--color-light);
    font-weight: 500;
    transition: color 0.3s ease;
  }
  
  nav a:hover {
    color: var(--color-primary);
  }
  
  /* ==============================
     HERO SECTION
     ============================== */
  #hero-index {
    min-height: 90vh;
    background:
      linear-gradient(rgba(11, 11, 15, 0.85), rgba(11, 11, 15, 0.85)),
      url("img/hero-tech.jpg") center / cover no-repeat;
    display: flex;
    justify-content: center;
    align-items: center;
    text-align: center;
  
    /* BONUS */
    background-attachment: fixed;
  }
  
  .hero-content {
    max-width: 45rem;
    padding: 2rem;
  }
  
  .hero-content h1 {
    font-size: 3rem;
    margin-bottom: 1rem;
  }
  
  .hero-content p {
    font-size: 1.25rem;
    color: var(--color-gray);
    margin-bottom: 2rem;
  }
  
  /* ==============================
     BOTONES
     ============================== */
  .btn-cta {
    display: inline-block;
    background-color: var(--color-primary);
    color: white;
    padding: 0.75rem 2rem;
    border-radius: 0.4rem;
    font-weight: 500;
    text-decoration: none;
    transition: background-color 0.3s ease, transform 0.3s ease;
  }
  
  .btn-cta:hover {
    background-color: var(--color-primary-light);
    transform: translateY(-0.2rem);
  }
  
  /* ==============================
     MAIN CONTENT
     ============================== */
  main {
    padding: 4rem 3rem;
  }
  
  section {
    margin-bottom: 4rem;
  }
  
  section h2 {
    font-size: 2rem;
    margin-bottom: 1.5rem;
    color: var(--color-primary-light);
  }
  
  /* ==============================
     INTRO
     ============================== */
  .intro p {
    max-width: 50rem;
    color: var(--color-gray);
  }
  
  /* ==============================
     SERVICIOS - GALERÍA FLEX
     ============================== */
  .galeria {
    display: flex;
    flex-wrap: wrap;
    gap: 2rem;
  }
  
  .card {
    background-color: var(--color-dark-soft);
    padding: 2rem;
    border-radius: 0.6rem;
    flex: 1 1 16rem;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }
  
  .card h3 {
    margin-bottom: 1rem;
    color: var(--color-primary);
  }
  
  .card p {
    color: var(--color-gray);
  }
  
  .card:hover {
    transform: translateY(-0.4rem);
    box-shadow: 0 0.8rem 2rem rgba(0, 0, 0, 0.6);
  }
  
  /* ==============================
     FOOTER
     ============================== */
  footer {
    background-color: var(--color-dark-soft);
    text-align: center;
    padding: 2rem;
    font-size: 0.9rem;
    color: var(--color-gray);
  }
  
  /* ==============================
     FORMULARIOS
     ============================== */
  form {
    max-width: 35rem;
  }
  
  label {
    display: block;
    margin-bottom: 0.5rem;
  }
  
  input,
  select,
  textarea {
    width: 100%;
    padding: 0.6rem;
    margin-bottom: 1.5rem;
    background-color: var(--color-dark);
    border: 1px solid var(--color-gray);
    color: var(--color-light);
    border-radius: 0.3rem;
    font-size: 1rem;
  }
  
  input:focus,
  select:focus,
  textarea:focus {
    outline: none;
    border-color: var(--color-primary);
  }
  
  input:invalid,
  textarea:invalid {
    border-color: #dc2626;
  }
  
  input:valid,
  textarea:valid {
    border-color: #16a34a;
  }
  
  /* ==============================
     TABLAS
     ============================== */
  table {
    width: 100%;
    border-collapse: collapse;
  }
  
  th,
  td {
    padding: 1rem;
    text-align: left;
  }
  
  thead {
    background-color: var(--color-primary);
  }
  
  tbody tr:nth-child(even) {
    background-color: var(--color-dark-soft);
  }
  
  /* ==============================
     LAYOUT PROYECTO (MAIN + ASIDE)
     ============================== */
  .layout-proyecto {
    display: flex;
    gap: 2rem;
  }
  
  .layout-proyecto main {
    flex-grow: 1;
  }
  
  .layout-proyecto aside {
    width: 18rem;
    background-color: var(--color-dark-soft);
    padding: 1.5rem;
    border-radius: 0.5rem;
  }
  
  /* ==============================
     RESPONSIVE DESIGN
     ============================== */
  /* Breakpoint principal - 768px */
  @media (max-width: 48rem) {
  
    nav ul {
      flex-direction: column;
      align-items: center;
    }
  
    #hero-index h1 {
      font-size: 2.2rem;
    }
  
    .galeria {
      flex-direction: column;
    }
  
    .layout-proyecto {
      flex-direction: column;
    }
  
    .layout-proyecto aside {
      width: 100%;
    }
  }
  #hero-index {
    min-height: 100vh;
  
    background-image:
      linear-gradient(
        rgba(0, 0, 0, 0.75),
        rgba(0, 0, 0, 0.75)
      ),
      url("ChatGPT\ Image\ Jan\ 9\,\ 2026\,\ 02\ 39\ 50\ PM.png");
  
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
  
    display: flex;
    justify-content: center;
    align-items: center;
    text-align: center;
  }
  
